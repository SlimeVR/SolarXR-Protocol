// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { DataFeedConfig, DataFeedConfigT } from '../../slimevr-protocol/data-feed/data-feed-config';


/**
 * Requests for the other party to send `data_feeds`.
 * For example, GUI requests for position data to be sent from server.
 *
 * When sending a new `DataFeedRequest`, the old data feeds will stop existing.
 * We still support multiple data feeds at the same time, because `data_feeds`
 * is a list.
 *
 * Multiple data feeds are useful to get data at different frequencies.
 */
export class DataFeedRequest {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
__init(i:number, bb:flatbuffers.ByteBuffer):DataFeedRequest {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsDataFeedRequest(bb:flatbuffers.ByteBuffer, obj?:DataFeedRequest):DataFeedRequest {
  return (obj || new DataFeedRequest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsDataFeedRequest(bb:flatbuffers.ByteBuffer, obj?:DataFeedRequest):DataFeedRequest {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new DataFeedRequest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

dataFeeds(index: number, obj?:DataFeedConfig):DataFeedConfig|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new DataFeedConfig()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

dataFeedsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startDataFeedRequest(builder:flatbuffers.Builder) {
  builder.startObject(1);
}

static addDataFeeds(builder:flatbuffers.Builder, dataFeedsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, dataFeedsOffset, 0);
}

static createDataFeedsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startDataFeedsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endDataFeedRequest(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createDataFeedRequest(builder:flatbuffers.Builder, dataFeedsOffset:flatbuffers.Offset):flatbuffers.Offset {
  DataFeedRequest.startDataFeedRequest(builder);
  DataFeedRequest.addDataFeeds(builder, dataFeedsOffset);
  return DataFeedRequest.endDataFeedRequest(builder);
}

unpack(): DataFeedRequestT {
  return new DataFeedRequestT(
    this.bb!.createObjList(this.dataFeeds.bind(this), this.dataFeedsLength())
  );
}


unpackTo(_o: DataFeedRequestT): void {
  _o.dataFeeds = this.bb!.createObjList(this.dataFeeds.bind(this), this.dataFeedsLength());
}
}

export class DataFeedRequestT {
constructor(
  public dataFeeds: (DataFeedConfigT)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const dataFeeds = DataFeedRequest.createDataFeedsVector(builder, builder.createObjectOffsetList(this.dataFeeds));

  return DataFeedRequest.createDataFeedRequest(builder,
    dataFeeds
  );
}
}
