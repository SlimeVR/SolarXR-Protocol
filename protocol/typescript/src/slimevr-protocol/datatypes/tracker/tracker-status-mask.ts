// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';



/**
 * A mask of the data in `TrackerStatus`
 */
export class TrackerStatusMask {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
__init(i:number, bb:flatbuffers.ByteBuffer):TrackerStatusMask {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsTrackerStatusMask(bb:flatbuffers.ByteBuffer, obj?:TrackerStatusMask):TrackerStatusMask {
  return (obj || new TrackerStatusMask()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsTrackerStatusMask(bb:flatbuffers.ByteBuffer, obj?:TrackerStatusMask):TrackerStatusMask {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new TrackerStatusMask()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

role():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

orientation():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

position():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

rawRotVel():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

rawTransAccel():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

temp():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

pollRate():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

mountingRotation():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

static startTrackerStatusMask(builder:flatbuffers.Builder) {
  builder.startObject(8);
}

static addRole(builder:flatbuffers.Builder, role:boolean) {
  builder.addFieldInt8(0, +role, +false);
}

static addOrientation(builder:flatbuffers.Builder, orientation:boolean) {
  builder.addFieldInt8(1, +orientation, +false);
}

static addPosition(builder:flatbuffers.Builder, position:boolean) {
  builder.addFieldInt8(2, +position, +false);
}

static addRawRotVel(builder:flatbuffers.Builder, rawRotVel:boolean) {
  builder.addFieldInt8(3, +rawRotVel, +false);
}

static addRawTransAccel(builder:flatbuffers.Builder, rawTransAccel:boolean) {
  builder.addFieldInt8(4, +rawTransAccel, +false);
}

static addTemp(builder:flatbuffers.Builder, temp:boolean) {
  builder.addFieldInt8(5, +temp, +false);
}

static addPollRate(builder:flatbuffers.Builder, pollRate:boolean) {
  builder.addFieldInt8(6, +pollRate, +false);
}

static addMountingRotation(builder:flatbuffers.Builder, mountingRotation:boolean) {
  builder.addFieldInt8(7, +mountingRotation, +false);
}

static endTrackerStatusMask(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createTrackerStatusMask(builder:flatbuffers.Builder, role:boolean, orientation:boolean, position:boolean, rawRotVel:boolean, rawTransAccel:boolean, temp:boolean, pollRate:boolean, mountingRotation:boolean):flatbuffers.Offset {
  TrackerStatusMask.startTrackerStatusMask(builder);
  TrackerStatusMask.addRole(builder, role);
  TrackerStatusMask.addOrientation(builder, orientation);
  TrackerStatusMask.addPosition(builder, position);
  TrackerStatusMask.addRawRotVel(builder, rawRotVel);
  TrackerStatusMask.addRawTransAccel(builder, rawTransAccel);
  TrackerStatusMask.addTemp(builder, temp);
  TrackerStatusMask.addPollRate(builder, pollRate);
  TrackerStatusMask.addMountingRotation(builder, mountingRotation);
  return TrackerStatusMask.endTrackerStatusMask(builder);
}

unpack(): TrackerStatusMaskT {
  return new TrackerStatusMaskT(
    this.role(),
    this.orientation(),
    this.position(),
    this.rawRotVel(),
    this.rawTransAccel(),
    this.temp(),
    this.pollRate(),
    this.mountingRotation()
  );
}


unpackTo(_o: TrackerStatusMaskT): void {
  _o.role = this.role();
  _o.orientation = this.orientation();
  _o.position = this.position();
  _o.rawRotVel = this.rawRotVel();
  _o.rawTransAccel = this.rawTransAccel();
  _o.temp = this.temp();
  _o.pollRate = this.pollRate();
  _o.mountingRotation = this.mountingRotation();
}
}

export class TrackerStatusMaskT {
constructor(
  public role: boolean = false,
  public orientation: boolean = false,
  public position: boolean = false,
  public rawRotVel: boolean = false,
  public rawTransAccel: boolean = false,
  public temp: boolean = false,
  public pollRate: boolean = false,
  public mountingRotation: boolean = false
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  return TrackerStatusMask.createTrackerStatusMask(builder,
    this.role,
    this.orientation,
    this.position,
    this.rawRotVel,
    this.rawTransAccel,
    this.temp,
    this.pollRate,
    this.mountingRotation
  );
}
}
