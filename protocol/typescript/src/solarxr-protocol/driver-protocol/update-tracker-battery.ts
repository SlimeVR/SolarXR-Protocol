// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';



/**
 * Update the battery information of a created tracker. If this is never sent, battery information will not be displayed.
 */
export class UpdateTrackerBattery implements flatbuffers.IUnpackableObject<UpdateTrackerBatteryT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):UpdateTrackerBattery {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsUpdateTrackerBattery(bb:flatbuffers.ByteBuffer, obj?:UpdateTrackerBattery):UpdateTrackerBattery {
  return (obj || new UpdateTrackerBattery()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsUpdateTrackerBattery(bb:flatbuffers.ByteBuffer, obj?:UpdateTrackerBattery):UpdateTrackerBattery {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new UpdateTrackerBattery()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * The ID of the tracker this is targeting. You should only target trackers you have created.
 */
trackerId():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

/**
 * The new battery level of the tracker. (0..=100)
 */
batteryLevel():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

/**
 * If the tracker is currently charging.
 */
charging():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

static startUpdateTrackerBattery(builder:flatbuffers.Builder) {
  builder.startObject(3);
}

static addTrackerId(builder:flatbuffers.Builder, trackerId:number) {
  builder.addFieldInt16(0, trackerId, 0);
}

static addBatteryLevel(builder:flatbuffers.Builder, batteryLevel:number) {
  builder.addFieldInt8(1, batteryLevel, 0);
}

static addCharging(builder:flatbuffers.Builder, charging:boolean) {
  builder.addFieldInt8(2, +charging, +false);
}

static endUpdateTrackerBattery(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createUpdateTrackerBattery(builder:flatbuffers.Builder, trackerId:number, batteryLevel:number, charging:boolean):flatbuffers.Offset {
  UpdateTrackerBattery.startUpdateTrackerBattery(builder);
  UpdateTrackerBattery.addTrackerId(builder, trackerId);
  UpdateTrackerBattery.addBatteryLevel(builder, batteryLevel);
  UpdateTrackerBattery.addCharging(builder, charging);
  return UpdateTrackerBattery.endUpdateTrackerBattery(builder);
}

unpack(): UpdateTrackerBatteryT {
  return new UpdateTrackerBatteryT(
    this.trackerId(),
    this.batteryLevel(),
    this.charging()
  );
}


unpackTo(_o: UpdateTrackerBatteryT): void {
  _o.trackerId = this.trackerId();
  _o.batteryLevel = this.batteryLevel();
  _o.charging = this.charging();
}
}

export class UpdateTrackerBatteryT implements flatbuffers.IGeneratedObject {
constructor(
  public trackerId: number = 0,
  public batteryLevel: number = 0,
  public charging: boolean = false
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  return UpdateTrackerBattery.createUpdateTrackerBattery(builder,
    this.trackerId,
    this.batteryLevel,
    this.charging
  );
}
}
