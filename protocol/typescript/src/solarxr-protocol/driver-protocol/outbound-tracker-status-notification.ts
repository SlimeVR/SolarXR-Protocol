// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { TrackerStatus } from '../../solarxr-protocol/datatypes/tracker-status.js';


export class OutboundTrackerStatusNotification implements flatbuffers.IUnpackableObject<OutboundTrackerStatusNotificationT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):OutboundTrackerStatusNotification {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsOutboundTrackerStatusNotification(bb:flatbuffers.ByteBuffer, obj?:OutboundTrackerStatusNotification):OutboundTrackerStatusNotification {
  return (obj || new OutboundTrackerStatusNotification()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsOutboundTrackerStatusNotification(bb:flatbuffers.ByteBuffer, obj?:OutboundTrackerStatusNotification):OutboundTrackerStatusNotification {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new OutboundTrackerStatusNotification()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

trackerId():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

status():TrackerStatus {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : TrackerStatus.NONE;
}

batteryLevel():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

charging():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

static startOutboundTrackerStatusNotification(builder:flatbuffers.Builder) {
  builder.startObject(4);
}

static addTrackerId(builder:flatbuffers.Builder, trackerId:number) {
  builder.addFieldInt16(0, trackerId, 0);
}

static addStatus(builder:flatbuffers.Builder, status:TrackerStatus) {
  builder.addFieldInt8(1, status, TrackerStatus.NONE);
}

static addBatteryLevel(builder:flatbuffers.Builder, batteryLevel:number) {
  builder.addFieldFloat32(2, batteryLevel, 0.0);
}

static addCharging(builder:flatbuffers.Builder, charging:boolean) {
  builder.addFieldInt8(3, +charging, +false);
}

static endOutboundTrackerStatusNotification(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createOutboundTrackerStatusNotification(builder:flatbuffers.Builder, trackerId:number, status:TrackerStatus, batteryLevel:number, charging:boolean):flatbuffers.Offset {
  OutboundTrackerStatusNotification.startOutboundTrackerStatusNotification(builder);
  OutboundTrackerStatusNotification.addTrackerId(builder, trackerId);
  OutboundTrackerStatusNotification.addStatus(builder, status);
  OutboundTrackerStatusNotification.addBatteryLevel(builder, batteryLevel);
  OutboundTrackerStatusNotification.addCharging(builder, charging);
  return OutboundTrackerStatusNotification.endOutboundTrackerStatusNotification(builder);
}

unpack(): OutboundTrackerStatusNotificationT {
  return new OutboundTrackerStatusNotificationT(
    this.trackerId(),
    this.status(),
    this.batteryLevel(),
    this.charging()
  );
}


unpackTo(_o: OutboundTrackerStatusNotificationT): void {
  _o.trackerId = this.trackerId();
  _o.status = this.status();
  _o.batteryLevel = this.batteryLevel();
  _o.charging = this.charging();
}
}

export class OutboundTrackerStatusNotificationT implements flatbuffers.IGeneratedObject {
constructor(
  public trackerId: number = 0,
  public status: TrackerStatus = TrackerStatus.NONE,
  public batteryLevel: number = 0.0,
  public charging: boolean = false
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  return OutboundTrackerStatusNotification.createOutboundTrackerStatusNotification(builder,
    this.trackerId,
    this.status,
    this.batteryLevel,
    this.charging
  );
}
}
