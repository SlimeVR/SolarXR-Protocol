// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { HandshakeStatus } from '../../solarxr-protocol/driver-protocol/handshake-status.js';


/**
 * Response to a HandshakeRequest. You may receive this message after the initial handshake if the server wishes to
 * stop communication with your driver for whatever reason, e.g. the user has disabled the driver in settings.
 */
export class HandshakeResponse implements flatbuffers.IUnpackableObject<HandshakeResponseT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):HandshakeResponse {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsHandshakeResponse(bb:flatbuffers.ByteBuffer, obj?:HandshakeResponse):HandshakeResponse {
  return (obj || new HandshakeResponse()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsHandshakeResponse(bb:flatbuffers.ByteBuffer, obj?:HandshakeResponse):HandshakeResponse {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new HandshakeResponse()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

status():HandshakeStatus {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : HandshakeStatus.ACCEPTED;
}

static startHandshakeResponse(builder:flatbuffers.Builder) {
  builder.startObject(1);
}

static addStatus(builder:flatbuffers.Builder, status:HandshakeStatus) {
  builder.addFieldInt8(0, status, HandshakeStatus.ACCEPTED);
}

static endHandshakeResponse(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createHandshakeResponse(builder:flatbuffers.Builder, status:HandshakeStatus):flatbuffers.Offset {
  HandshakeResponse.startHandshakeResponse(builder);
  HandshakeResponse.addStatus(builder, status);
  return HandshakeResponse.endHandshakeResponse(builder);
}

unpack(): HandshakeResponseT {
  return new HandshakeResponseT(
    this.status()
  );
}


unpackTo(_o: HandshakeResponseT): void {
  _o.status = this.status();
}
}

export class HandshakeResponseT implements flatbuffers.IGeneratedObject {
constructor(
  public status: HandshakeStatus = HandshakeStatus.ACCEPTED
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  return HandshakeResponse.createHandshakeResponse(builder,
    this.status
  );
}
}
