// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { DetectedDevice, DetectedDeviceT } from '../../../solarxr-protocol/application/rpc/detected-device';


export class DetectedDevicesRequest {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
__init(i:number, bb:flatbuffers.ByteBuffer):DetectedDevicesRequest {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsDetectedDevicesRequest(bb:flatbuffers.ByteBuffer, obj?:DetectedDevicesRequest):DetectedDevicesRequest {
  return (obj || new DetectedDevicesRequest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsDetectedDevicesRequest(bb:flatbuffers.ByteBuffer, obj?:DetectedDevicesRequest):DetectedDevicesRequest {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new DetectedDevicesRequest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

devices(index: number, obj?:DetectedDevice):DetectedDevice|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new DetectedDevice()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

devicesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startDetectedDevicesRequest(builder:flatbuffers.Builder) {
  builder.startObject(1);
}

static addDevices(builder:flatbuffers.Builder, devicesOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, devicesOffset, 0);
}

static createDevicesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startDevicesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endDetectedDevicesRequest(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createDetectedDevicesRequest(builder:flatbuffers.Builder, devicesOffset:flatbuffers.Offset):flatbuffers.Offset {
  DetectedDevicesRequest.startDetectedDevicesRequest(builder);
  DetectedDevicesRequest.addDevices(builder, devicesOffset);
  return DetectedDevicesRequest.endDetectedDevicesRequest(builder);
}

unpack(): DetectedDevicesRequestT {
  return new DetectedDevicesRequestT(
    this.bb!.createObjList(this.devices.bind(this), this.devicesLength())
  );
}


unpackTo(_o: DetectedDevicesRequestT): void {
  _o.devices = this.bb!.createObjList(this.devices.bind(this), this.devicesLength());
}
}

export class DetectedDevicesRequestT {
constructor(
  public devices: (DetectedDeviceT)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const devices = DetectedDevicesRequest.createDevicesVector(builder, builder.createObjectOffsetList(this.devices));

  return DetectedDevicesRequest.createDetectedDevicesRequest(builder,
    devices
  );
}
}
