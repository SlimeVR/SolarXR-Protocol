// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { BodyPart } from '../../solarxr-protocol/datatypes/body-part.js';
import { VideoTrackerCalibrationCamera, VideoTrackerCalibrationCameraT } from '../../solarxr-protocol/rpc/video-tracker-calibration-camera.js';
import { VideoTrackerCalibrationStatus } from '../../solarxr-protocol/rpc/video-tracker-calibration-status.js';


export class VideoTrackerCalibrationProgressResponse implements flatbuffers.IUnpackableObject<VideoTrackerCalibrationProgressResponseT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):VideoTrackerCalibrationProgressResponse {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsVideoTrackerCalibrationProgressResponse(bb:flatbuffers.ByteBuffer, obj?:VideoTrackerCalibrationProgressResponse):VideoTrackerCalibrationProgressResponse {
  return (obj || new VideoTrackerCalibrationProgressResponse()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsVideoTrackerCalibrationProgressResponse(bb:flatbuffers.ByteBuffer, obj?:VideoTrackerCalibrationProgressResponse):VideoTrackerCalibrationProgressResponse {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new VideoTrackerCalibrationProgressResponse()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

status():VideoTrackerCalibrationStatus {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : VideoTrackerCalibrationStatus.CALIBRATE_CAMERA;
}

camera(obj?:VideoTrackerCalibrationCamera):VideoTrackerCalibrationCamera|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new VideoTrackerCalibrationCamera()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

trackersDone(index: number):BodyPart|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

trackersDoneLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

trackersDoneArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

trackersPending(index: number):BodyPart|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

trackersPendingLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

trackersPendingArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

error():string|null
error(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
error(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startVideoTrackerCalibrationProgressResponse(builder:flatbuffers.Builder) {
  builder.startObject(5);
}

static addStatus(builder:flatbuffers.Builder, status:VideoTrackerCalibrationStatus) {
  builder.addFieldInt8(0, status, VideoTrackerCalibrationStatus.CALIBRATE_CAMERA);
}

static addCamera(builder:flatbuffers.Builder, cameraOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, cameraOffset, 0);
}

static addTrackersDone(builder:flatbuffers.Builder, trackersDoneOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, trackersDoneOffset, 0);
}

static createTrackersDoneVector(builder:flatbuffers.Builder, data:BodyPart[]):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startTrackersDoneVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addTrackersPending(builder:flatbuffers.Builder, trackersPendingOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, trackersPendingOffset, 0);
}

static createTrackersPendingVector(builder:flatbuffers.Builder, data:BodyPart[]):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startTrackersPendingVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addError(builder:flatbuffers.Builder, errorOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, errorOffset, 0);
}

static endVideoTrackerCalibrationProgressResponse(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}


unpack(): VideoTrackerCalibrationProgressResponseT {
  return new VideoTrackerCalibrationProgressResponseT(
    this.status(),
    (this.camera() !== null ? this.camera()!.unpack() : null),
    this.bb!.createScalarList<BodyPart>(this.trackersDone.bind(this), this.trackersDoneLength()),
    this.bb!.createScalarList<BodyPart>(this.trackersPending.bind(this), this.trackersPendingLength()),
    this.error()
  );
}


unpackTo(_o: VideoTrackerCalibrationProgressResponseT): void {
  _o.status = this.status();
  _o.camera = (this.camera() !== null ? this.camera()!.unpack() : null);
  _o.trackersDone = this.bb!.createScalarList<BodyPart>(this.trackersDone.bind(this), this.trackersDoneLength());
  _o.trackersPending = this.bb!.createScalarList<BodyPart>(this.trackersPending.bind(this), this.trackersPendingLength());
  _o.error = this.error();
}
}

export class VideoTrackerCalibrationProgressResponseT implements flatbuffers.IGeneratedObject {
constructor(
  public status: VideoTrackerCalibrationStatus = VideoTrackerCalibrationStatus.CALIBRATE_CAMERA,
  public camera: VideoTrackerCalibrationCameraT|null = null,
  public trackersDone: (BodyPart)[] = [],
  public trackersPending: (BodyPart)[] = [],
  public error: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const camera = (this.camera !== null ? this.camera!.pack(builder) : 0);
  const trackersDone = VideoTrackerCalibrationProgressResponse.createTrackersDoneVector(builder, this.trackersDone);
  const trackersPending = VideoTrackerCalibrationProgressResponse.createTrackersPendingVector(builder, this.trackersPending);
  const error = (this.error !== null ? builder.createString(this.error!) : 0);

  VideoTrackerCalibrationProgressResponse.startVideoTrackerCalibrationProgressResponse(builder);
  VideoTrackerCalibrationProgressResponse.addStatus(builder, this.status);
  VideoTrackerCalibrationProgressResponse.addCamera(builder, camera);
  VideoTrackerCalibrationProgressResponse.addTrackersDone(builder, trackersDone);
  VideoTrackerCalibrationProgressResponse.addTrackersPending(builder, trackersPending);
  VideoTrackerCalibrationProgressResponse.addError(builder, error);

  return VideoTrackerCalibrationProgressResponse.endVideoTrackerCalibrationProgressResponse(builder);
}
}
