// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';



export class FolderPath implements flatbuffers.IUnpackableObject<FolderPathT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):FolderPath {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsFolderPath(bb:flatbuffers.ByteBuffer, obj?:FolderPath):FolderPath {
  return (obj || new FolderPath()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsFolderPath(bb:flatbuffers.ByteBuffer, obj?:FolderPath):FolderPath {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new FolderPath()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

value():string|null
value(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
value(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startFolderPath(builder:flatbuffers.Builder) {
  builder.startObject(1);
}

static addValue(builder:flatbuffers.Builder, valueOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, valueOffset, 0);
}

static endFolderPath(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createFolderPath(builder:flatbuffers.Builder, valueOffset:flatbuffers.Offset):flatbuffers.Offset {
  FolderPath.startFolderPath(builder);
  FolderPath.addValue(builder, valueOffset);
  return FolderPath.endFolderPath(builder);
}

unpack(): FolderPathT {
  return new FolderPathT(
    this.value()
  );
}


unpackTo(_o: FolderPathT): void {
  _o.value = this.value();
}
}

export class FolderPathT implements flatbuffers.IGeneratedObject {
constructor(
  public value: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const value = (this.value !== null ? builder.createString(this.value!) : 0);

  return FolderPath.createFolderPath(builder,
    value
  );
}
}
