// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { BodyPart } from '../../solarxr-protocol/datatypes/body-part.js';


/**
 * Request the server to add a tracker.
 */
export class AddTrackerRequest implements flatbuffers.IUnpackableObject<AddTrackerRequestT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):AddTrackerRequest {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsAddTrackerRequest(bb:flatbuffers.ByteBuffer, obj?:AddTrackerRequest):AddTrackerRequest {
  return (obj || new AddTrackerRequest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsAddTrackerRequest(bb:flatbuffers.ByteBuffer, obj?:AddTrackerRequest):AddTrackerRequest {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new AddTrackerRequest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

name():string|null
name(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
name(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

displayName():string|null
displayName(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
displayName(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

manufacturer():string|null
manufacturer(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
manufacturer(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

tracksRotation():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

tracksAcceleration():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

tracksPosition():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

roleHint():BodyPart|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : null;
}

isHmd():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

static startAddTrackerRequest(builder:flatbuffers.Builder) {
  builder.startObject(8);
}

static addName(builder:flatbuffers.Builder, nameOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, nameOffset, 0);
}

static addDisplayName(builder:flatbuffers.Builder, displayNameOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, displayNameOffset, 0);
}

static addManufacturer(builder:flatbuffers.Builder, manufacturerOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, manufacturerOffset, 0);
}

static addTracksRotation(builder:flatbuffers.Builder, tracksRotation:boolean) {
  builder.addFieldInt8(3, +tracksRotation, +false);
}

static addTracksAcceleration(builder:flatbuffers.Builder, tracksAcceleration:boolean) {
  builder.addFieldInt8(4, +tracksAcceleration, +false);
}

static addTracksPosition(builder:flatbuffers.Builder, tracksPosition:boolean) {
  builder.addFieldInt8(5, +tracksPosition, +false);
}

static addRoleHint(builder:flatbuffers.Builder, roleHint:BodyPart) {
  builder.addFieldInt8(6, roleHint, 0);
}

static addIsHmd(builder:flatbuffers.Builder, isHmd:boolean) {
  builder.addFieldInt8(7, +isHmd, +false);
}

static endAddTrackerRequest(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createAddTrackerRequest(builder:flatbuffers.Builder, nameOffset:flatbuffers.Offset, displayNameOffset:flatbuffers.Offset, manufacturerOffset:flatbuffers.Offset, tracksRotation:boolean, tracksAcceleration:boolean, tracksPosition:boolean, roleHint:BodyPart|null, isHmd:boolean):flatbuffers.Offset {
  AddTrackerRequest.startAddTrackerRequest(builder);
  AddTrackerRequest.addName(builder, nameOffset);
  AddTrackerRequest.addDisplayName(builder, displayNameOffset);
  AddTrackerRequest.addManufacturer(builder, manufacturerOffset);
  AddTrackerRequest.addTracksRotation(builder, tracksRotation);
  AddTrackerRequest.addTracksAcceleration(builder, tracksAcceleration);
  AddTrackerRequest.addTracksPosition(builder, tracksPosition);
  if (roleHint !== null)
    AddTrackerRequest.addRoleHint(builder, roleHint);
  AddTrackerRequest.addIsHmd(builder, isHmd);
  return AddTrackerRequest.endAddTrackerRequest(builder);
}

unpack(): AddTrackerRequestT {
  return new AddTrackerRequestT(
    this.name(),
    this.displayName(),
    this.manufacturer(),
    this.tracksRotation(),
    this.tracksAcceleration(),
    this.tracksPosition(),
    this.roleHint(),
    this.isHmd()
  );
}


unpackTo(_o: AddTrackerRequestT): void {
  _o.name = this.name();
  _o.displayName = this.displayName();
  _o.manufacturer = this.manufacturer();
  _o.tracksRotation = this.tracksRotation();
  _o.tracksAcceleration = this.tracksAcceleration();
  _o.tracksPosition = this.tracksPosition();
  _o.roleHint = this.roleHint();
  _o.isHmd = this.isHmd();
}
}

export class AddTrackerRequestT implements flatbuffers.IGeneratedObject {
constructor(
  public name: string|Uint8Array|null = null,
  public displayName: string|Uint8Array|null = null,
  public manufacturer: string|Uint8Array|null = null,
  public tracksRotation: boolean = false,
  public tracksAcceleration: boolean = false,
  public tracksPosition: boolean = false,
  public roleHint: BodyPart|null = null,
  public isHmd: boolean = false
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const name = (this.name !== null ? builder.createString(this.name!) : 0);
  const displayName = (this.displayName !== null ? builder.createString(this.displayName!) : 0);
  const manufacturer = (this.manufacturer !== null ? builder.createString(this.manufacturer!) : 0);

  return AddTrackerRequest.createAddTrackerRequest(builder,
    name,
    displayName,
    manufacturer,
    this.tracksRotation,
    this.tracksAcceleration,
    this.tracksPosition,
    this.roleHint,
    this.isHmd
  );
}
}
