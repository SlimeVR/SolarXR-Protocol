// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { FlightListStep, FlightListStepT } from '../../solarxr-protocol/rpc/flight-list-step.js';
import { FlightListStepId } from '../../solarxr-protocol/rpc/flight-list-step-id.js';


export class FlightListResponse implements flatbuffers.IUnpackableObject<FlightListResponseT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):FlightListResponse {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsFlightListResponse(bb:flatbuffers.ByteBuffer, obj?:FlightListResponse):FlightListResponse {
  return (obj || new FlightListResponse()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsFlightListResponse(bb:flatbuffers.ByteBuffer, obj?:FlightListResponse):FlightListResponse {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new FlightListResponse()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

steps(index: number, obj?:FlightListStep):FlightListStep|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new FlightListStep()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

stepsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

ignoredSteps(index: number):FlightListStepId|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

ignoredStepsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

ignoredStepsArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

static startFlightListResponse(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addSteps(builder:flatbuffers.Builder, stepsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, stepsOffset, 0);
}

static createStepsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startStepsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addIgnoredSteps(builder:flatbuffers.Builder, ignoredStepsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, ignoredStepsOffset, 0);
}

static createIgnoredStepsVector(builder:flatbuffers.Builder, data:FlightListStepId[]):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startIgnoredStepsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static endFlightListResponse(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createFlightListResponse(builder:flatbuffers.Builder, stepsOffset:flatbuffers.Offset, ignoredStepsOffset:flatbuffers.Offset):flatbuffers.Offset {
  FlightListResponse.startFlightListResponse(builder);
  FlightListResponse.addSteps(builder, stepsOffset);
  FlightListResponse.addIgnoredSteps(builder, ignoredStepsOffset);
  return FlightListResponse.endFlightListResponse(builder);
}

unpack(): FlightListResponseT {
  return new FlightListResponseT(
    this.bb!.createObjList<FlightListStep, FlightListStepT>(this.steps.bind(this), this.stepsLength()),
    this.bb!.createScalarList<FlightListStepId>(this.ignoredSteps.bind(this), this.ignoredStepsLength())
  );
}


unpackTo(_o: FlightListResponseT): void {
  _o.steps = this.bb!.createObjList<FlightListStep, FlightListStepT>(this.steps.bind(this), this.stepsLength());
  _o.ignoredSteps = this.bb!.createScalarList<FlightListStepId>(this.ignoredSteps.bind(this), this.ignoredStepsLength());
}
}

export class FlightListResponseT implements flatbuffers.IGeneratedObject {
constructor(
  public steps: (FlightListStepT)[] = [],
  public ignoredSteps: (FlightListStepId)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const steps = FlightListResponse.createStepsVector(builder, builder.createObjectOffsetList(this.steps));
  const ignoredSteps = FlightListResponse.createIgnoredStepsVector(builder, this.ignoredSteps);

  return FlightListResponse.createFlightListResponse(builder,
    steps,
    ignoredSteps
  );
}
}
