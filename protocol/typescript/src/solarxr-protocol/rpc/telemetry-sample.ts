// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';



export class TelemetrySample implements flatbuffers.IUnpackableObject<TelemetrySampleT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):TelemetrySample {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsTelemetrySample(bb:flatbuffers.ByteBuffer, obj?:TelemetrySample):TelemetrySample {
  return (obj || new TelemetrySample()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsTelemetrySample(bb:flatbuffers.ByteBuffer, obj?:TelemetrySample):TelemetrySample {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new TelemetrySample()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

deviceId():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

time():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

rssi():number|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readInt16(this.bb_pos + offset) : null;
}

packetsLost():number|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : null;
}

packetsReceived():number|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : null;
}

rssiMin():number|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readInt16(this.bb_pos + offset) : null;
}

rssiMax():number|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readInt16(this.bb_pos + offset) : null;
}

packetLossPct():number|null {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : null;
}

static startTelemetrySample(builder:flatbuffers.Builder) {
  builder.startObject(8);
}

static addDeviceId(builder:flatbuffers.Builder, deviceId:number) {
  builder.addFieldInt16(0, deviceId, 0);
}

static addTime(builder:flatbuffers.Builder, time:bigint) {
  builder.addFieldInt64(1, time, BigInt('0'));
}

static addRssi(builder:flatbuffers.Builder, rssi:number) {
  builder.addFieldInt16(2, rssi, 0);
}

static addPacketsLost(builder:flatbuffers.Builder, packetsLost:number) {
  builder.addFieldInt32(3, packetsLost, 0);
}

static addPacketsReceived(builder:flatbuffers.Builder, packetsReceived:number) {
  builder.addFieldInt32(4, packetsReceived, 0);
}

static addRssiMin(builder:flatbuffers.Builder, rssiMin:number) {
  builder.addFieldInt16(5, rssiMin, 0);
}

static addRssiMax(builder:flatbuffers.Builder, rssiMax:number) {
  builder.addFieldInt16(6, rssiMax, 0);
}

static addPacketLossPct(builder:flatbuffers.Builder, packetLossPct:number) {
  builder.addFieldFloat32(7, packetLossPct, 0);
}

static endTelemetrySample(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createTelemetrySample(builder:flatbuffers.Builder, deviceId:number, time:bigint, rssi:number|null, packetsLost:number|null, packetsReceived:number|null, rssiMin:number|null, rssiMax:number|null, packetLossPct:number|null):flatbuffers.Offset {
  TelemetrySample.startTelemetrySample(builder);
  TelemetrySample.addDeviceId(builder, deviceId);
  TelemetrySample.addTime(builder, time);
  if (rssi !== null)
    TelemetrySample.addRssi(builder, rssi);
  if (packetsLost !== null)
    TelemetrySample.addPacketsLost(builder, packetsLost);
  if (packetsReceived !== null)
    TelemetrySample.addPacketsReceived(builder, packetsReceived);
  if (rssiMin !== null)
    TelemetrySample.addRssiMin(builder, rssiMin);
  if (rssiMax !== null)
    TelemetrySample.addRssiMax(builder, rssiMax);
  if (packetLossPct !== null)
    TelemetrySample.addPacketLossPct(builder, packetLossPct);
  return TelemetrySample.endTelemetrySample(builder);
}

unpack(): TelemetrySampleT {
  return new TelemetrySampleT(
    this.deviceId(),
    this.time(),
    this.rssi(),
    this.packetsLost(),
    this.packetsReceived(),
    this.rssiMin(),
    this.rssiMax(),
    this.packetLossPct()
  );
}


unpackTo(_o: TelemetrySampleT): void {
  _o.deviceId = this.deviceId();
  _o.time = this.time();
  _o.rssi = this.rssi();
  _o.packetsLost = this.packetsLost();
  _o.packetsReceived = this.packetsReceived();
  _o.rssiMin = this.rssiMin();
  _o.rssiMax = this.rssiMax();
  _o.packetLossPct = this.packetLossPct();
}
}

export class TelemetrySampleT implements flatbuffers.IGeneratedObject {
constructor(
  public deviceId: number = 0,
  public time: bigint = BigInt('0'),
  public rssi: number|null = null,
  public packetsLost: number|null = null,
  public packetsReceived: number|null = null,
  public rssiMin: number|null = null,
  public rssiMax: number|null = null,
  public packetLossPct: number|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  return TelemetrySample.createTelemetrySample(builder,
    this.deviceId,
    this.time,
    this.rssi,
    this.packetsLost,
    this.packetsReceived,
    this.rssiMin,
    this.rssiMax,
    this.packetLossPct
  );
}
}
