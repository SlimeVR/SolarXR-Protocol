// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { ScalingValues, ScalingValuesT } from '../../../solarxr-protocol/rpc/settings/scaling-values.js';
import { VelocityPreset } from '../../../solarxr-protocol/rpc/settings/velocity-preset.js';
import { VelocityScalingPreset } from '../../../solarxr-protocol/rpc/settings/velocity-scaling-preset.js';


/**
 * Settings for derived velocity data sent via Protobuf.
 * Enables Natural Locomotion support and similar locomotion systems.
 */
export class VelocitySettings implements flatbuffers.IUnpackableObject<VelocitySettingsT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):VelocitySettings {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsVelocitySettings(bb:flatbuffers.ByteBuffer, obj?:VelocitySettings):VelocitySettings {
  return (obj || new VelocitySettings()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsVelocitySettings(bb:flatbuffers.ByteBuffer, obj?:VelocitySettings):VelocitySettings {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new VelocitySettings()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Global toggle to enable/disable sending derived velocity for all trackers
 */
sendDerivedVelocity():boolean|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : null;
}

/**
 * Which tracker role groups should expose velocity
 */
preset():VelocityPreset|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : null;
}

/**
 * Enabled role groups when preset is CUSTOM (bitmask representation)
 * Bit 0 = FEET, Bit 1 = ANKLES, Bit 2 = KNEES, Bit 3 = CHEST, Bit 4 = WAIST, Bit 5 = ELBOWS
 */
enabledGroups():number|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : null;
}

/**
 * Override the default scaling preset binding to role presets
 */
overrideScalingPreset():boolean|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : null;
}

/**
 * Velocity scaling preset
 */
scalingPreset():VelocityScalingPreset|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : null;
}

/**
 * Allow scaling values > 1.0 (up to 5.0)
 * WARNING: FBT position prediction may break. This is an accessibility feature.
 */
enableUpscaling():boolean|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : null;
}

/**
 * Velocity scaling factors (1.0 = no scaling)
 */
scale(obj?:ScalingValues):ScalingValues|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? (obj || new ScalingValues()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

static startVelocitySettings(builder:flatbuffers.Builder) {
  builder.startObject(7);
}

static addSendDerivedVelocity(builder:flatbuffers.Builder, sendDerivedVelocity:boolean) {
  builder.addFieldInt8(0, +sendDerivedVelocity, 0);
}

static addPreset(builder:flatbuffers.Builder, preset:VelocityPreset) {
  builder.addFieldInt8(1, preset, 0);
}

static addEnabledGroups(builder:flatbuffers.Builder, enabledGroups:number) {
  builder.addFieldInt8(2, enabledGroups, 0);
}

static addOverrideScalingPreset(builder:flatbuffers.Builder, overrideScalingPreset:boolean) {
  builder.addFieldInt8(3, +overrideScalingPreset, 0);
}

static addScalingPreset(builder:flatbuffers.Builder, scalingPreset:VelocityScalingPreset) {
  builder.addFieldInt8(4, scalingPreset, 0);
}

static addEnableUpscaling(builder:flatbuffers.Builder, enableUpscaling:boolean) {
  builder.addFieldInt8(5, +enableUpscaling, 0);
}

static addScale(builder:flatbuffers.Builder, scaleOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, scaleOffset, 0);
}

static endVelocitySettings(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}


unpack(): VelocitySettingsT {
  return new VelocitySettingsT(
    this.sendDerivedVelocity(),
    this.preset(),
    this.enabledGroups(),
    this.overrideScalingPreset(),
    this.scalingPreset(),
    this.enableUpscaling(),
    (this.scale() !== null ? this.scale()!.unpack() : null)
  );
}


unpackTo(_o: VelocitySettingsT): void {
  _o.sendDerivedVelocity = this.sendDerivedVelocity();
  _o.preset = this.preset();
  _o.enabledGroups = this.enabledGroups();
  _o.overrideScalingPreset = this.overrideScalingPreset();
  _o.scalingPreset = this.scalingPreset();
  _o.enableUpscaling = this.enableUpscaling();
  _o.scale = (this.scale() !== null ? this.scale()!.unpack() : null);
}
}

export class VelocitySettingsT implements flatbuffers.IGeneratedObject {
constructor(
  public sendDerivedVelocity: boolean|null = null,
  public preset: VelocityPreset|null = null,
  public enabledGroups: number|null = null,
  public overrideScalingPreset: boolean|null = null,
  public scalingPreset: VelocityScalingPreset|null = null,
  public enableUpscaling: boolean|null = null,
  public scale: ScalingValuesT|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const scale = (this.scale !== null ? this.scale!.pack(builder) : 0);

  VelocitySettings.startVelocitySettings(builder);
  if (this.sendDerivedVelocity !== null)
    VelocitySettings.addSendDerivedVelocity(builder, this.sendDerivedVelocity);
  if (this.preset !== null)
    VelocitySettings.addPreset(builder, this.preset);
  if (this.enabledGroups !== null)
    VelocitySettings.addEnabledGroups(builder, this.enabledGroups);
  if (this.overrideScalingPreset !== null)
    VelocitySettings.addOverrideScalingPreset(builder, this.overrideScalingPreset);
  if (this.scalingPreset !== null)
    VelocitySettings.addScalingPreset(builder, this.scalingPreset);
  if (this.enableUpscaling !== null)
    VelocitySettings.addEnableUpscaling(builder, this.enableUpscaling);
  VelocitySettings.addScale(builder, scale);

  return VelocitySettings.endVelocitySettings(builder);
}
}
