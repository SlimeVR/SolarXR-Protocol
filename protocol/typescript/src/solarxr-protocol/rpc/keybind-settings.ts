// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { Keybind, KeybindT } from '../../solarxr-protocol/rpc/keybind.js';


export class KeybindSettings implements flatbuffers.IUnpackableObject<KeybindSettingsT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):KeybindSettings {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsKeybindSettings(bb:flatbuffers.ByteBuffer, obj?:KeybindSettings):KeybindSettings {
  return (obj || new KeybindSettings()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsKeybindSettings(bb:flatbuffers.ByteBuffer, obj?:KeybindSettings):KeybindSettings {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new KeybindSettings()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

name():string|null
name(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
name(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

keybind(obj?:Keybind):Keybind|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new Keybind()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

static startKeybindSettings(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addName(builder:flatbuffers.Builder, nameOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, nameOffset, 0);
}

static addKeybind(builder:flatbuffers.Builder, keybindOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, keybindOffset, 0);
}

static endKeybindSettings(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}


unpack(): KeybindSettingsT {
  return new KeybindSettingsT(
    this.name(),
    (this.keybind() !== null ? this.keybind()!.unpack() : null)
  );
}


unpackTo(_o: KeybindSettingsT): void {
  _o.name = this.name();
  _o.keybind = (this.keybind() !== null ? this.keybind()!.unpack() : null);
}
}

export class KeybindSettingsT implements flatbuffers.IGeneratedObject {
constructor(
  public name: string|Uint8Array|null = null,
  public keybind: KeybindT|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const name = (this.name !== null ? builder.createString(this.name!) : 0);
  const keybind = (this.keybind !== null ? this.keybind!.pack(builder) : 0);

  KeybindSettings.startKeybindSettings(builder);
  KeybindSettings.addName(builder, name);
  KeybindSettings.addKeybind(builder, keybind);

  return KeybindSettings.endKeybindSettings(builder);
}
}
