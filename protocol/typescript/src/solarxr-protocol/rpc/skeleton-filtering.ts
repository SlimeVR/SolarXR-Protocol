// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { FilteringType } from '../../solarxr-protocol/rpc/filtering-type.js';


/**
 * Filtering (e.g smoothing) applied to the skeleton's movements
 */
export class SkeletonFiltering implements flatbuffers.IUnpackableObject<SkeletonFilteringT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):SkeletonFiltering {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsSkeletonFiltering(bb:flatbuffers.ByteBuffer, obj?:SkeletonFiltering):SkeletonFiltering {
  return (obj || new SkeletonFiltering()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsSkeletonFiltering(bb:flatbuffers.ByteBuffer, obj?:SkeletonFiltering):SkeletonFiltering {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new SkeletonFiltering()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

type():FilteringType {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : FilteringType.NONE;
}

/**
 * A higher value results in more filtering (0 to 1).
 */
amount():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

static startSkeletonFiltering(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addType(builder:flatbuffers.Builder, type:FilteringType) {
  builder.addFieldInt8(0, type, FilteringType.NONE);
}

static addAmount(builder:flatbuffers.Builder, amount:number) {
  builder.addFieldFloat32(1, amount, 0.0);
}

static endSkeletonFiltering(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createSkeletonFiltering(builder:flatbuffers.Builder, type:FilteringType, amount:number):flatbuffers.Offset {
  SkeletonFiltering.startSkeletonFiltering(builder);
  SkeletonFiltering.addType(builder, type);
  SkeletonFiltering.addAmount(builder, amount);
  return SkeletonFiltering.endSkeletonFiltering(builder);
}

unpack(): SkeletonFilteringT {
  return new SkeletonFilteringT(
    this.type(),
    this.amount()
  );
}


unpackTo(_o: SkeletonFilteringT): void {
  _o.type = this.type();
  _o.amount = this.amount();
}
}

export class SkeletonFilteringT implements flatbuffers.IGeneratedObject {
constructor(
  public type: FilteringType = FilteringType.NONE,
  public amount: number = 0.0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  return SkeletonFiltering.createSkeletonFiltering(builder,
    this.type,
    this.amount
  );
}
}
