// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { BoneRoute, BoneRouteT } from '../../solarxr-protocol/rpc/bone-route.js';
import { RoutingOutputStatus, RoutingOutputStatusT } from '../../solarxr-protocol/rpc/routing-output-status.js';


export class BoneRoutingSettingsResponse implements flatbuffers.IUnpackableObject<BoneRoutingSettingsResponseT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):BoneRoutingSettingsResponse {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsBoneRoutingSettingsResponse(bb:flatbuffers.ByteBuffer, obj?:BoneRoutingSettingsResponse):BoneRoutingSettingsResponse {
  return (obj || new BoneRoutingSettingsResponse()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsBoneRoutingSettingsResponse(bb:flatbuffers.ByteBuffer, obj?:BoneRoutingSettingsResponse):BoneRoutingSettingsResponse {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new BoneRoutingSettingsResponse()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * When set, `routes` is generated from the connected trackers and the
 * output priority, and any stored manual routes are ignored.
 */
automatic():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

/**
 * The effective routes, whether generated or stored.
 */
routes(index: number, obj?:BoneRoute):BoneRoute|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new BoneRoute()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

routesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Always one entry per output. `state` says what is up with each.
 */
outputs(index: number, obj?:RoutingOutputStatus):RoutingOutputStatus|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? (obj || new RoutingOutputStatus()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

outputsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startBoneRoutingSettingsResponse(builder:flatbuffers.Builder) {
  builder.startObject(3);
}

static addAutomatic(builder:flatbuffers.Builder, automatic:boolean) {
  builder.addFieldInt8(0, +automatic, +false);
}

static addRoutes(builder:flatbuffers.Builder, routesOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, routesOffset, 0);
}

static createRoutesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startRoutesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addOutputs(builder:flatbuffers.Builder, outputsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, outputsOffset, 0);
}

static createOutputsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startOutputsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endBoneRoutingSettingsResponse(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createBoneRoutingSettingsResponse(builder:flatbuffers.Builder, automatic:boolean, routesOffset:flatbuffers.Offset, outputsOffset:flatbuffers.Offset):flatbuffers.Offset {
  BoneRoutingSettingsResponse.startBoneRoutingSettingsResponse(builder);
  BoneRoutingSettingsResponse.addAutomatic(builder, automatic);
  BoneRoutingSettingsResponse.addRoutes(builder, routesOffset);
  BoneRoutingSettingsResponse.addOutputs(builder, outputsOffset);
  return BoneRoutingSettingsResponse.endBoneRoutingSettingsResponse(builder);
}

unpack(): BoneRoutingSettingsResponseT {
  return new BoneRoutingSettingsResponseT(
    this.automatic(),
    this.bb!.createObjList<BoneRoute, BoneRouteT>(this.routes.bind(this), this.routesLength()),
    this.bb!.createObjList<RoutingOutputStatus, RoutingOutputStatusT>(this.outputs.bind(this), this.outputsLength())
  );
}


unpackTo(_o: BoneRoutingSettingsResponseT): void {
  _o.automatic = this.automatic();
  _o.routes = this.bb!.createObjList<BoneRoute, BoneRouteT>(this.routes.bind(this), this.routesLength());
  _o.outputs = this.bb!.createObjList<RoutingOutputStatus, RoutingOutputStatusT>(this.outputs.bind(this), this.outputsLength());
}
}

export class BoneRoutingSettingsResponseT implements flatbuffers.IGeneratedObject {
constructor(
  public automatic: boolean = false,
  public routes: (BoneRouteT)[] = [],
  public outputs: (RoutingOutputStatusT)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const routes = BoneRoutingSettingsResponse.createRoutesVector(builder, builder.createObjectOffsetList(this.routes));
  const outputs = BoneRoutingSettingsResponse.createOutputsVector(builder, builder.createObjectOffsetList(this.outputs));

  return BoneRoutingSettingsResponse.createBoneRoutingSettingsResponse(builder,
    this.automatic,
    routes,
    outputs
  );
}
}
