// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';



export class TrackerInfoMask {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
__init(i:number, bb:flatbuffers.ByteBuffer):TrackerInfoMask {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsTrackerInfoMask(bb:flatbuffers.ByteBuffer, obj?:TrackerInfoMask):TrackerInfoMask {
  return (obj || new TrackerInfoMask()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsTrackerInfoMask(bb:flatbuffers.ByteBuffer, obj?:TrackerInfoMask):TrackerInfoMask {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new TrackerInfoMask()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

name():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

imuType():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

bodyPart():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

pollRate():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

mountingOrientation():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

editable():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

computed():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

static startTrackerInfoMask(builder:flatbuffers.Builder) {
  builder.startObject(7);
}

static addName(builder:flatbuffers.Builder, name:boolean) {
  builder.addFieldInt8(0, +name, +false);
}

static addImuType(builder:flatbuffers.Builder, imuType:boolean) {
  builder.addFieldInt8(1, +imuType, +false);
}

static addBodyPart(builder:flatbuffers.Builder, bodyPart:boolean) {
  builder.addFieldInt8(2, +bodyPart, +false);
}

static addPollRate(builder:flatbuffers.Builder, pollRate:boolean) {
  builder.addFieldInt8(3, +pollRate, +false);
}

static addMountingOrientation(builder:flatbuffers.Builder, mountingOrientation:boolean) {
  builder.addFieldInt8(4, +mountingOrientation, +false);
}

static addEditable(builder:flatbuffers.Builder, editable:boolean) {
  builder.addFieldInt8(5, +editable, +false);
}

static addComputed(builder:flatbuffers.Builder, computed:boolean) {
  builder.addFieldInt8(6, +computed, +false);
}

static endTrackerInfoMask(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createTrackerInfoMask(builder:flatbuffers.Builder, name:boolean, imuType:boolean, bodyPart:boolean, pollRate:boolean, mountingOrientation:boolean, editable:boolean, computed:boolean):flatbuffers.Offset {
  TrackerInfoMask.startTrackerInfoMask(builder);
  TrackerInfoMask.addName(builder, name);
  TrackerInfoMask.addImuType(builder, imuType);
  TrackerInfoMask.addBodyPart(builder, bodyPart);
  TrackerInfoMask.addPollRate(builder, pollRate);
  TrackerInfoMask.addMountingOrientation(builder, mountingOrientation);
  TrackerInfoMask.addEditable(builder, editable);
  TrackerInfoMask.addComputed(builder, computed);
  return TrackerInfoMask.endTrackerInfoMask(builder);
}

unpack(): TrackerInfoMaskT {
  return new TrackerInfoMaskT(
    this.name(),
    this.imuType(),
    this.bodyPart(),
    this.pollRate(),
    this.mountingOrientation(),
    this.editable(),
    this.computed()
  );
}


unpackTo(_o: TrackerInfoMaskT): void {
  _o.name = this.name();
  _o.imuType = this.imuType();
  _o.bodyPart = this.bodyPart();
  _o.pollRate = this.pollRate();
  _o.mountingOrientation = this.mountingOrientation();
  _o.editable = this.editable();
  _o.computed = this.computed();
}
}

export class TrackerInfoMaskT {
constructor(
  public name: boolean = false,
  public imuType: boolean = false,
  public bodyPart: boolean = false,
  public pollRate: boolean = false,
  public mountingOrientation: boolean = false,
  public editable: boolean = false,
  public computed: boolean = false
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  return TrackerInfoMask.createTrackerInfoMask(builder,
    this.name,
    this.imuType,
    this.bodyPart,
    this.pollRate,
    this.mountingOrientation,
    this.editable,
    this.computed
  );
}
}
