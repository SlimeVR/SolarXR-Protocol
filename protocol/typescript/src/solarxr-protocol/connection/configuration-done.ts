// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';



/**
 * Ends one peer's half of the configuration phase, and is required from both: the
 * client sends it after its last request, the server after its last answer and only
 * once it has seen the client's. Until a peer sends it, it may spread configuration
 * over further bundles. Application messages flow once both peers have sent it.
 */
export class ConfigurationDone implements flatbuffers.IUnpackableObject<ConfigurationDoneT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):ConfigurationDone {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsConfigurationDone(bb:flatbuffers.ByteBuffer, obj?:ConfigurationDone):ConfigurationDone {
  return (obj || new ConfigurationDone()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsConfigurationDone(bb:flatbuffers.ByteBuffer, obj?:ConfigurationDone):ConfigurationDone {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new ConfigurationDone()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static startConfigurationDone(builder:flatbuffers.Builder) {
  builder.startObject(0);
}

static endConfigurationDone(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createConfigurationDone(builder:flatbuffers.Builder):flatbuffers.Offset {
  ConfigurationDone.startConfigurationDone(builder);
  return ConfigurationDone.endConfigurationDone(builder);
}

unpack(): ConfigurationDoneT {
  return new ConfigurationDoneT();
}


unpackTo(_o: ConfigurationDoneT): void {}
}

export class ConfigurationDoneT implements flatbuffers.IGeneratedObject {
constructor(){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  return ConfigurationDone.createConfigurationDone(builder);
}
}
