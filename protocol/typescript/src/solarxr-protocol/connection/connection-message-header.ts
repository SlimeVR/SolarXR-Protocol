// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { BoneRegistry, BoneRegistryT } from '../../solarxr-protocol/connection/bone-registry.js';
import { ConfigurationAcknowledged, ConfigurationAcknowledgedT } from '../../solarxr-protocol/connection/configuration-acknowledged.js';
import { ConnectionError, ConnectionErrorT } from '../../solarxr-protocol/connection/connection-error.js';
import { ConnectionMessage, unionToConnectionMessage, unionListToConnectionMessage } from '../../solarxr-protocol/connection/connection-message.js';
import { FinishConfiguration, FinishConfigurationT } from '../../solarxr-protocol/connection/finish-configuration.js';


export class ConnectionMessageHeader implements flatbuffers.IUnpackableObject<ConnectionMessageHeaderT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):ConnectionMessageHeader {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsConnectionMessageHeader(bb:flatbuffers.ByteBuffer, obj?:ConnectionMessageHeader):ConnectionMessageHeader {
  return (obj || new ConnectionMessageHeader()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsConnectionMessageHeader(bb:flatbuffers.ByteBuffer, obj?:ConnectionMessageHeader):ConnectionMessageHeader {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new ConnectionMessageHeader()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

messageType():ConnectionMessage {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : ConnectionMessage.NONE;
}

message<T extends flatbuffers.Table>(obj:any):any|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__union(obj, this.bb_pos + offset) : null;
}

static startConnectionMessageHeader(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addMessageType(builder:flatbuffers.Builder, messageType:ConnectionMessage) {
  builder.addFieldInt8(0, messageType, ConnectionMessage.NONE);
}

static addMessage(builder:flatbuffers.Builder, messageOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, messageOffset, 0);
}

static endConnectionMessageHeader(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createConnectionMessageHeader(builder:flatbuffers.Builder, messageType:ConnectionMessage, messageOffset:flatbuffers.Offset):flatbuffers.Offset {
  ConnectionMessageHeader.startConnectionMessageHeader(builder);
  ConnectionMessageHeader.addMessageType(builder, messageType);
  ConnectionMessageHeader.addMessage(builder, messageOffset);
  return ConnectionMessageHeader.endConnectionMessageHeader(builder);
}

unpack(): ConnectionMessageHeaderT {
  return new ConnectionMessageHeaderT(
    this.messageType(),
    (() => {
      const temp = unionToConnectionMessage(this.messageType(), this.message.bind(this));
      if(temp === null) { return null; }
      return temp.unpack()
  })()
  );
}


unpackTo(_o: ConnectionMessageHeaderT): void {
  _o.messageType = this.messageType();
  _o.message = (() => {
      const temp = unionToConnectionMessage(this.messageType(), this.message.bind(this));
      if(temp === null) { return null; }
      return temp.unpack()
  })();
}
}

export class ConnectionMessageHeaderT implements flatbuffers.IGeneratedObject {
constructor(
  public messageType: ConnectionMessage = ConnectionMessage.NONE,
  public message: BoneRegistryT|ConfigurationAcknowledgedT|ConnectionErrorT|FinishConfigurationT|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const message = builder.createObjectOffset(this.message);

  return ConnectionMessageHeader.createConnectionMessageHeader(builder,
    this.messageType,
    message
  );
}
}
