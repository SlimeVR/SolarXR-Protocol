// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { BoneDefinition, BoneDefinitionT } from '../../solarxr-protocol/connection/bone-definition.js';


/**
 * Authoritative, connection-wide bone identity and hierarchy: a flat list where
 * each definition names its own parent. IDs are compact registry-local handles;
 * persist keys and resolve them after reconnecting.
 */
export class BoneRegistry implements flatbuffers.IUnpackableObject<BoneRegistryT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):BoneRegistry {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsBoneRegistry(bb:flatbuffers.ByteBuffer, obj?:BoneRegistry):BoneRegistry {
  return (obj || new BoneRegistry()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsBoneRegistry(bb:flatbuffers.ByteBuffer, obj?:BoneRegistry):BoneRegistry {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new BoneRegistry()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

bones(index: number, obj?:BoneDefinition):BoneDefinition|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new BoneDefinition()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

bonesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startBoneRegistry(builder:flatbuffers.Builder) {
  builder.startObject(1);
}

static addBones(builder:flatbuffers.Builder, bonesOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, bonesOffset, 0);
}

static createBonesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startBonesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endBoneRegistry(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // bones
  return offset;
}

static createBoneRegistry(builder:flatbuffers.Builder, bonesOffset:flatbuffers.Offset):flatbuffers.Offset {
  BoneRegistry.startBoneRegistry(builder);
  BoneRegistry.addBones(builder, bonesOffset);
  return BoneRegistry.endBoneRegistry(builder);
}

unpack(): BoneRegistryT {
  return new BoneRegistryT(
    this.bb!.createObjList<BoneDefinition, BoneDefinitionT>(this.bones.bind(this), this.bonesLength())
  );
}


unpackTo(_o: BoneRegistryT): void {
  _o.bones = this.bb!.createObjList<BoneDefinition, BoneDefinitionT>(this.bones.bind(this), this.bonesLength());
}
}

export class BoneRegistryT implements flatbuffers.IGeneratedObject {
constructor(
  public bones: (BoneDefinitionT)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const bones = BoneRegistry.createBonesVector(builder, builder.createObjectOffsetList(this.bones));

  return BoneRegistry.createBoneRegistry(builder,
    bones
  );
}
}
